[pytest]
asyncio_mode = auto
# Scope collection to the real suite -- the repo root also has a few
# standalone test_*.py scripts (ad-hoc manual verification against a
# running server, not pytest tests) that pytest's default rootdir-wide
# collection would otherwise try and fail to import.
testpaths = tests
